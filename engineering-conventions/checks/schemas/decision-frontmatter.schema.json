{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Decision record frontmatter",
  "description": "The YAML frontmatter of an architecture decision record in docs/decisions/NNNN-slug.md.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "title", "date", "status"],
  "properties": {
    "id": {
      "description": "The four-digit decision number, as a string so leading zeros survive, e.g. \"0004\". It matches the filename prefix.",
      "$ref": "#/definitions/decisionId"
    },
    "title": {
      "description": "The decision, in sentence case.",
      "$ref": "common.schema.json#/definitions/nonEmptyString"
    },
    "date": {
      "description": "When the decision reached its current status.",
      "$ref": "common.schema.json#/definitions/date"
    },
    "status": {
      "description": "proposed: open for comment; accepted: in force; rejected: considered and declined; deprecated: no longer applies; superseded: replaced by the decisions in superseded_by.",
      "type": "string",
      "enum": ["proposed", "accepted", "rejected", "deprecated", "superseded"]
    },
    "deciders": {
      "description": "Who made the decision.",
      "type": "array",
      "uniqueItems": true,
      "items": { "$ref": "common.schema.json#/definitions/nonEmptyString" }
    },
    "consulted": {
      "description": "Whose opinion was sought before deciding.",
      "type": "array",
      "uniqueItems": true,
      "items": { "$ref": "common.schema.json#/definitions/nonEmptyString" }
    },
    "informed": {
      "description": "Who is kept up to date on the decision.",
      "type": "array",
      "uniqueItems": true,
      "items": { "$ref": "common.schema.json#/definitions/nonEmptyString" }
    },
    "supersedes": {
      "description": "Decisions this one replaces.",
      "type": "array",
      "uniqueItems": true,
      "items": { "$ref": "#/definitions/decisionId" }
    },
    "superseded_by": {
      "description": "Decisions that replace this one. Required once the status is superseded.",
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "$ref": "#/definitions/decisionId" }
    }
  },
  "if": {
    "required": ["status"],
    "properties": { "status": { "const": "superseded" } }
  },
  "then": {
    "required": ["superseded_by"]
  },
  "definitions": {
    "decisionId": {
      "type": "string",
      "pattern": "^[0-9]{4}$"
    }
  }
}
