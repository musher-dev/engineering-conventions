{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Outputs declaration",
  "description": "A repository's .repo/outputs.yaml: every output it publishes for other repositories or people to consume, where it is built from, which workflow publishes it, where consumers get it and where its consumption is documented. OUT-02 checks a declaration against this schema.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "outputs"],
  "properties": {
    "schema_version": {
      "description": "Version of this file's shape. Always 1.",
      "const": 1
    },
    "outputs": {
      "description": "The outputs this repository publishes, at least one.",
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/definitions/output" }
    }
  },
  "definitions": {
    "output": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "kind", "source", "publish_workflow", "location", "docs"],
      "properties": {
        "id": {
          "description": "The output's name within this repository, unique, e.g. api-image.",
          "$ref": "common.schema.json#/definitions/slug"
        },
        "kind": {
          "description": "What kind of output it is: an output-kind token from the terminology, e.g. image, library, cli, contract or bundle. OUT-03 checks the token.",
          "type": "string",
          "pattern": "^[a-z][a-z0-9]*$"
        },
        "description": {
          "description": "One line on what the output is for.",
          "$ref": "common.schema.json#/definitions/nonEmptyString"
        },
        "source": {
          "description": "The file or directory it is built from, relative to the repository root, e.g. api/ or engineering-conventions/.",
          "$ref": "common.schema.json#/definitions/repoPath"
        },
        "publish_workflow": {
          "description": "The filename of the workflow under .github/workflows that publishes it, e.g. publish-api.yml.",
          "type": "string",
          "pattern": "^[^/\\s]+\\.(yml|yaml)$"
        },
        "location": {
          "description": "Where a consumer gets it: an image repository, a package name in its registry, or a release URL, e.g. ghcr.io/musher-dev/api.",
          "$ref": "common.schema.json#/definitions/nonEmptyString"
        },
        "docs": {
          "description": "The document that says how to consume it, relative to the repository root, with an optional #anchor, e.g. api/README.md#consuming.",
          "$ref": "common.schema.json#/definitions/repoPath"
        },
        "format": {
          "description": "A contract's interface format, e.g. openapi, asyncapi, protobuf or json-schema. Required when kind is contract (OUT-07).",
          "$ref": "common.schema.json#/definitions/slug"
        },
        "definition": {
          "description": "A contract's machine-readable definition file, relative to the repository root, e.g. api/openapi.yaml. Required when kind is contract (OUT-07).",
          "$ref": "common.schema.json#/definitions/repoPath"
        }
      }
    }
  }
}
