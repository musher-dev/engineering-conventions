name: Validate
on:
  pull_request:
permissions: {}
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  links:
    name: Docs Links
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check the links
        run: echo links
